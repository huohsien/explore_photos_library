### Desired new REPORT-01 status model

Define a dedicated centralized enum or equivalent official definitions for REPORT-01 only:

```text
NO_PROBLEM
MISSING_ALBUM_IN_CURRENT
MISSING_BACKUP_MEMBERS
AMBIGUOUS_CURRENT_ALBUM_PATH
```

Definitions:

```text
NO_PROBLEM
- Current has one unambiguous corresponding AlbumPath.
- It contains every Backup member.
- Current extra members do not make it a problem.

MISSING_ALBUM_IN_CURRENT
- Backup has this AlbumPath.
- Current has no album container at that AlbumPath.

MISSING_BACKUP_MEMBERS
- Current has one unambiguous corresponding AlbumPath.
- At least one Backup member is absent from it.

AMBIGUOUS_CURRENT_ALBUM_PATH
- Current has 2+ different album container objects at the same canonical AlbumPath.
- Do not auto-repair until the duplicate-container ambiguity is resolved.
```

Current-only AlbumPaths may be optional diagnostics or an appendix statistic, but they are not part of the Backup restoration repair queue.

The redesigned report should be driven primarily by Backup AlbumPaths, not by the union of Backup and Current paths.

## Old REPORT-01 baseline on 2026-06-22 around 11:12

```text
Backup unique AlbumPaths: 5,110
Current unique AlbumPaths: 5,974
All unique AlbumPaths: 5,980

MISSING_ALBUM_IN_CURRENT: 6
MEMBERSHIP_DIFF: 13
CURRENT_ONLY_ALBUM: 870
OK: 5,091
```

The 13 `MEMBERSHIP_DIFF` rows must be decomposed into:

```text
MISSING_BACKUP_MEMBERS
or
NO_PROBLEM with Current additions
```

after the redesign.

## Duplicate album container cleanup: side project

This work was done because duplicate Current album containers at one AlbumPath cause `AMBIGUOUS_CURRENT_ALBUM_PATH`, making structural repair unsafe.

Safe process:

```text
Probe exact AlbumPath
→ choose master
→ additive merge source memberships to master
→ verify expected unique union
→ delete source album containers only
→ rebuild and verify
```

Scripts intended as final versions:

```text
REPORT-07_Exact-AlbumPath-Probe-and-Spotlight-step1.applescript
REPORT-07_AlbumPath_Additive-Merge_With-Manual-Master-Choice-step2.applescript
REPORT-07_AlbumPath_Delete-Merged-Source-Albums-step3.applescript
```

Verified earlier:

```text
宵夜
- duplicate containers consolidated in Backup and Current
- final membership: 120

HIDE / Girls
- duplicate containers consolidated in Backup and Current
- final membership: 742
```

Many additional duplicate AlbumPaths were then manually processed in both libraries.

A REPORT-07 run showed only:

```text
HIDE / Nudity
```

remaining before final cleanup. User reports that final `HIDE / Nudity` cleanup was performed manually in both libraries, but no fresh rebuild / REPORT-07 zero-duplicate verification has yet been captured. Do not claim duplicate paths are verified zero until that rebuild occurs.

Do not automatically start cross-path semantic organization such as:

```text
[ROOT_ALBUMS] / Girls
→ HIDE / Girls
```

That is a separate optional side project, not main crash restoration.

## Prior main repair pilot

`股票` was the first root-folder repair pilot.

The approach established:

```text
move existing matching Current albums first
→ create truly missing albums later
→ rebuild and audit
```

Reported preflight work included:

```text
already at target path: 276
need create / missing: 27
multiple exact matches: 2
```

A create-missing dry run reported approximately:

```text
ready jobs: 27
asset rows: 247
indexed PHAssets: about 95,597
```

Some manual corrections were also done under `股票`.

Do not assume old counts are still current; a fresh redesigned REPORT-01 and repair preflight are authoritative.


---------------


A. Current 同一 AlbumPath 有多個 album containers：3 組

這三組會讓 REPORT-01 刻意跳過 membership 比較；所以它們是驗證上的 blocker。

Current AlbumPath	Current albums
[ROOT_ALBUMS] / CamScanner	2
[ROOT_ALBUMS] / Girls	5
[ROOT_ALBUMS] / 素人自拍 公共場合裸露（畫面模糊 馬賽克）	2

Backup 端這三個 path 都各只有一個 album，Current 才有重複 shell。現在不能假設任一個 container 可以直接刪；正確作法仍是你已經做過的同路徑流程：probe → 選 master → additive merge memberships → verify union → delete source album containers，而且只刪 album containers、不刪 PHAssets。

這三組應該先清掉，因為只要它們還存在，REPORT-01 無法告訴我們這三個 canonical path 裡是否少了 Backup-required members。

B. Backup 有、Current 完全沒有的 AlbumPath：6 組

這不代表照片遺失；因為全庫 assets 是 0 missing。它代表：Backup 中的 album container / AlbumPath 與其 membership，沒有在 Current 的正確位置存在。

Root	缺少的 Backup AlbumPath	Backup members
Root	2016年跟小乖去日本玩照片的殘存檔	238
Root	Home Improvement	892
Root	Kagyu 2017	523
Root	因為小乖自作聰明，把我一個多月前沒有參加瑜伽會所所說的話現在才實現。昨天炒飯居然沒有用飯是用花椰菜的假飯！搞得我澱粉缺乏。晚上已經要吃第三餐了，第二餐是一度讚牛肉麵，還是沒有飽。現在跑最後一個南洋進口份量感覺更少的蛋黃醬炒麵… 泡好把醬拌起來，聞起來超香的！  --  在7-11（不是每一家都有）買的外國泡麵，口味蠻特殊的。沒有什麼料但是要$37。可能是東南亞的…	10
Root	瑜珈身體部位名稱	1
股票	股票 / #ETF #00940之亂… 長標題 album	2

這六組才是既有 REPAIR-01 → REPAIR-02 → REPAIR-03 / REPAIR-04 的正確使用情境：

REPAIR-01
找出 Backup target 的結構與候選資訊

REPAIR-02
檢查該 target 是否已經在 Current 的別處存在，
避免誤 create 或誤 move

REPAIR-03
只有在 strict MOVE_READY 時，才 move existing Current album

REPAIR-04
只有在 target 確實不存在、又沒有可以安全移回的 existing album 時，才 create missing album

不能看見 AlbumPath Missing 就直接 create；那些 238、892、523 張的 assets 可能已在 Current，只是 album container 被放錯位置或 membership 分散在別的 album。

C. Current path 存在，但少了 Backup-required membership：4 組

這四組的 album path 已正確存在；問題是 Backup 當時放進該 album 的一些 asset，沒有被放進 Current 同一路徑的 album。

AlbumPath	Backup	Current	缺少
Root 的「大姑姑家／大姑丈往生49天／2025-02-08」長標題 album	13	11	2
HIDE / Girls-3	1,012	1,011	1
HIDE / 外送茶莊性感美女	128	20	108
Root 𝕏	4	20	3

尤其最後那個 𝕏 很容易誤判：Current 有 20 張，不代表它包含 Backup 的 4 張。實際上它少了 Backup 的其中 3 張，只是同時多了很多別的 Current-only members。

這一類不能用 REPAIR-03 move，也不能用 REPAIR-04 create，更不能用 DeleteDuplicate。它需要一個獨立、正確的新 stage：

REPAIR-05 — Add Backup-Required Members to Existing Current Albums

它的責任必須很窄：

對每個已存在的 Current AlbumPath：
只把報表列出的 Backup-required、但該 Current album 尚未包含的 asset 加進去；
保留 Current 原有的 extra members；
不移動 album；
不刪除 PHAsset；
先 dry run、再小批次 execute、最後 rebuild / REPORT-01 驗證。

這個才是處理 HIDE / 外送茶莊性感美女 缺 108 張的正確工具；不能再把它亂塞到 REPAIR-02 或 REPAIR-03。

正確處理順序

我會建議這樣走，邏輯最乾淨：

1. 先處理 A：
   Current 同一路徑重複 album containers 的三組
   CamScanner / Girls / 素人自拍…
   → 因為它們目前遮蔽 membership validation。

2. 重建 Current inventory，重跑 REPORT-01。
   → 這時會首次得到那三個 path 的真實 Backup-member 結果。

3. 處理 B：
   六個 Backup AlbumPaths Missing from Current
   → 用既有 REPAIR-01 → REPAIR-02
   → 每筆再分流到 REPAIR-03 move 或 REPAIR-04 create。

4. 處理 C：
   所有 existing-path、但缺 Backup-required membership 的 paths
   → 正式建立 REPAIR-05 的 manifest/export + Swift dry-run executor。

5. 再 rebuild、再跑 whole-library REPORT-01。

最後剩下的 metadata 問題是獨立的小尾巴：

Keyword missing: 1
- IMG_0014.PNG 缺 NSFW_IG

Adjustment metadata differences: 2
- IMG_3003.JPG
- IMG_3004.JPG

它們不影響目前 album structure 的處理順序。

現在最應該先動的是 A：三組 Current same-AlbumPath duplicate containers；把這三組處理完，下一次 REPORT-01 才不會有被跳過的 membership comparison。